# Local dev only -- in production these are set as Fly.io secrets
# (fly secrets set --config voice-agent-service/fly.toml --app heimdell-voice-agent KEY=value),
# never committed or put in fly.toml itself.

PORT=8080

# Added in Phase C -- not required for the Phase A echo scaffold:
# DATABASE_URL=
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# ANTHROPIC_API_KEY=
# APP_URL=
